import { AgregarEmojiPipe } from './agregar-emoji-pipe';

describe('AgregarEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new AgregarEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
